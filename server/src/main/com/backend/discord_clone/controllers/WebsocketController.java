package com.backend.discord_clone.Controllers;

import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.messaging.handler.annotation.MessageMapping;
import org.springframework.stereotype.Controller;



@Controller
public class WebsocketController {
    // private final LocationService _locationService;

    // public WebsocketController(LocationService locationService) {
    //     this._locationService = locationService;
    // }

    // @MessageMapping("/updateLocation")
    // public ResponseEntity<LocationUpdateResponse> updateLocation(LocationUpdateRequest locationUpdateRequest) {
    //     return new ResponseEntity<>(_locationService.updateLocation(locationUpdateRequest), HttpStatus.OK);
    // }

}
